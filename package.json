{
  "name": "exhibit-browserify",
  "version": "0.1.0",
  "author": "Callum Locke",
  "dependencies": {
    "babel-runtime": "^5.6.15",
    "browser-resolve-noio": "^1.4.1",
    "browserify": "^10.2.4",
    "defined": "^1.0.0",
    "has": "^1.0.0",
    "insert-module-globals": "^6.5.1",
    "module-deps-diskless": "^3.8.1",
    "streamifier": "^0.1.1",
    "xtend": "^4.0.0"
  },
  "keywords": [
    "exhibit-plugin"
  ],
  "license": "MIT",
  "main": "lib",
  "scripts": {
    "build": "rimraf lib && babel src --source-maps --optional runtime --out-dir lib",
    "prepublish": "npm run build",
    "watch": "nodemon --exec 'npm run build' --watch src --watch index.js --watch package.json"
  },
  "devDependencies": {
    "babel": "^5.5.6",
    "babel-eslint": "^3.1.14",
    "eslint": "^0.22.1",
    "nodemon": "^1.3.7",
    "rimraf": "^2.4.1"
  }
}
